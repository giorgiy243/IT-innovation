# Ядро платформы. Все зависимости - self-hosted, пермиссивные лицензии (см. AI/platform/самодостаточность.md).
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
pydantic>=2
python-dotenv>=1
PyYAML>=6
SQLAlchemy>=2
alembic>=1.13
passlib[argon2]>=1.7
psycopg[binary]>=3.1   # драйвер PostgreSQL (прод)
# Тесты
pytest>=8
httpx>=0.27
