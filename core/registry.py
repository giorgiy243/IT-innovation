"""Реестр модулей.

Ядро не знает о модулях по имени - модули регистрируются здесь. Добавление модуля
не трогает ядро (см. AI/platform/архитектура.md). По реестру строится навигация
с учётом RBAC (роль видит модуль -> пункт меню).

Скелет подфазы 0.3. Реализация - Фаза 1 (вместе с первым модулем Продажи).
"""


def register(module_code: str, module) -> None:
    """Зарегистрировать модуль в платформе. Реализация - Фаза 1."""
    raise NotImplementedError("Фаза 1: регистрация модуля в реестре")


def enabled_modules(role_codes: list[str]) -> list[str]:
    """Модули, доступные пользователю по его ролям. Реализация - Фаза 1."""
    raise NotImplementedError("Фаза 1: фильтрация модулей по role_modules")
